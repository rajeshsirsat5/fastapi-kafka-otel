I want to debug the OpenTelemetry issue that is there in my setup. In my setup I have two environments: one is PCF and the other is AWS. PCF has a service that is a Spring Boot service, which has an endpoint. When I hit that endpoint, it publishes the data to one of the topics. Another service in AWS consumes the topic, processes the data, and publishes to a target topic. I really wanted to see the OpenTelemetry trace from this HTTP endpoint all the way to the target topic.

In the current setup the PCF service sends telemetry data to the PCF OpenTelemetry collector. The AWS service sends the data to the AWS collector. The PCF collector will forward all the telemetry to the AWS collector and at the end the AWS collector will have telemetry for both services. Now I'm using Grafana to listen to all the events from this AWS collector. I'm using Prometheus, CloudWatch X-Ray, and Cloud Log for all the telemetry data. I could see the logs and metrics but I am not able to see the trace for the entire trace when I hit the endpoint.

This is one issue and another issue is that I should see the trace within the service as well. I mean to say the second service, which consumes the data, at least should show me the trace saying "data is consumed, transformed, and published to the target topic". I don't see both the traces in AWS X-Ray and the Grafana setup. What could be the possible issue that I want you to debug into? 
